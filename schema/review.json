{
  "hotelId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Hotel" },
  "guestId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Guest" },
  "rating": { "type": "number", "required": true, "description": "Rating given (1-5)", "minimum": 1, "maximum": 5 },
  "comment": { "type": "string", "required": false, "description": "Guest's comment for the review" }
}