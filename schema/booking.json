{
  "hotelId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Hotel" },
  "guestId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Guest" },
  "roomId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Room" },
  "checkInDate": { "type": "date", "required": true, "description": "Check-in date" },
  "checkOutDate": { "type": "date", "required": true, "description": "Check-out date" },
  "numGuests": { "type": "number", "required": true, "description": "Number of guests" },
  "totalPrice": { "type": "number", "required": true, "description": "Total price of the booking" },
  "currency": { "type": "string", "required": true, "description": "Currency (e.g., 'USD')" },
  "status": { "type": "string", "required": true, "description": "Booking status (e.g., PENDING, CONFIRMED, CHECKED_IN, CHECKED_OUT, CANCELLED, NO_SHOW) - will be an enum later" },
  "notes": { "type": "string", "required": false, "description": "Any special notes about the booking" }
}