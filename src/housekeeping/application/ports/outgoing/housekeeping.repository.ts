import { Housekeeping } from 'src/housekeeping/domain/entities';
import { BaseRepository } from "@shared/shared-kernel";

export abstract class THousekeepingRepository extends BaseRepository<Housekeeping> {
}
