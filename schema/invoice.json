{
  "hotelId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Hotel" },
  "bookingId": { "type": "string", "required": true, "format": "uuid", "description": "Foreign key to Booking" },
  "invoiceNumber": { "type": "string", "required": true, "description": "Unique invoice number" },
  "amountDue": { "type": "number", "required": true, "description": "Total amount due on the invoice" },
  "taxes": { "type": "string", "required": false, "description": "JSON string representing tax breakdown (e.g., {\"VAT\": 0.10, \"City_Tax\": 0.02}) - will be an object later" },
  "issuedAt": { "type": "date", "required": true, "description": "Date when the invoice was issued" },
  "dueDate": { "type": "date", "required": true, "description": "Date by which the invoice is due" },
  "status": { "type": "string", "required": true, "description": "Invoice status (e.g., PAID, UNPAID) - will be an enum later" }
}